services:
  - type: web
    name: emailflow
    env: python
    buildCommand: "pip install -e ."
    startCommand: "gunicorn --bind 0.0.0.0:\$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        value: 10000

