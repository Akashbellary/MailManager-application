services:
  - type: web
    name: emailflow
    env: python
    buildCommand: "pip install -e ."
    startCommand: "bash start.sh"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: PORT
        value: 10000

